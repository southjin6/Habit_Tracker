# Copy this file to `.env` and fill in your own values.
# `.env` is gitignored and must never be committed.

# MySQL connection used by the app and by `dotnet ef` migrations.
# The database and user are created by setup.sql.
HABIT_CONNECTION=Server=localhost;Port=3306;Database=habit_tracker;User=habit_user;Password=CHANGE_ME;
